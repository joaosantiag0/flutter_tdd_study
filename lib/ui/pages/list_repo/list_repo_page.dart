import 'package:flutter/widgets.dart';

class ListRepoPage extends StatelessWidget {
  const ListRepoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
